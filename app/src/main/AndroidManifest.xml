<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.development.nest.studyshechdular">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyShechdular">
        <activity android:name=".ui.CalculatorActivity"></activity>
        <activity android:name=".ui.CgpaActivity" />
        <activity android:name=".ui.NoteUpdateActivity" />
        <activity android:name=".ui.CreateNoteActivity" />
        <activity android:name=".ui.NewScheduleActivity" />
        <activity android:name=".ui.ScheduleUpdateActivity" />
        <activity android:name=".ui.SchedulerActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.NotesActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>